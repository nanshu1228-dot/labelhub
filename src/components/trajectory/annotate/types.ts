/**
 * Annotation-UI value types.
 *
 * These shapes flow from the SSR loader (page.tsx) into the
 * <TrajectoryAnnotator> client shell. The shell normalizes them once and
 * passes plain objects to its children — no DB row shapes leak into React.
 *
 * Why a separate file: keeps types serializable (no Date instances, no
 * Drizzle-specific helpers), which means we can also use these for Storybook
 * fixtures and unit tests without dragging in db/schema.ts.
 */

import type { Mark, RubricSpec, TrajectoryStepKind } from '@/lib/templates/rubric'

export type { Mark, RubricSpec, TrajectoryStepKind }

/**
 * One step in the annotator's view of a trajectory.
 *
 * `content` is kind-discriminated jsonb in the DB; we type-narrow it on the
 * adapter side (see `from-db.ts`) so the UI gets a clean discriminated union.
 */
export type StepView =
  | {
      id: string
      sequence: number
      kind: 'thinking' | 'final_response' | 'sub_agent_response' | 'error'
      body: string
      latencyMs: number | null
      tokensIn: number | null
      tokensOut: number | null
      modelName: string | null
    }
  | {
      id: string
      sequence: number
      kind: 'tool_call' | 'sub_agent_call'
      toolName: string
      args: unknown
      toolCallId: string | null
      toolProvider: { name: string; kind: string } | null
      latencyMs: number | null
      tokensIn: number | null
      tokensOut: number | null
      modelName: string | null
    }
  | {
      id: string
      sequence: number
      kind: 'tool_result'
      toolName: string
      output: unknown
      toolCallId: string | null
      toolProvider: { name: string; kind: string } | null
      latencyMs: number | null
      tokensIn: number | null
      tokensOut: number | null
      modelName: string | null
    }

export interface TrajectoryView {
  id: string
  agentName: string
  modelName: string | null
  rootPrompt: string
  systemPrompt: string | null
  finalResponse: string | null
  steps: readonly StepView[]
  attachments: readonly AttachmentRef[]
}

export interface AttachmentRef {
  messageIndex: number
  blockIndex: number
  kind: 'image' | 'document' | 'audio'
  mediaType?: string
  url?: string
  bytes?: number
  hashPrefix?: string
}

/**
 * One peer rater's mark on a single rubric for a step. Renders as a colored
 * dot with the rater's initials; clusters with conflicting values are how
 * the heat-map strip computes "disputed" segments.
 */
export interface PeerMark {
  peerId: string
  peerInitials: string
  color: string
  rubricId: string
  value: number | string | boolean
}

/** Map: stepId → rubricId → peer marks (multiple peers may have rated the same rubric). */
export type PeerMarksByStep = Readonly<
  Record<string, Readonly<Record<string, readonly PeerMark[]>>>
>

/**
 * Claude pre-annotation hint surfaced as a dashed-border card under the rubric
 * input. The annotator can "Use" or "Override". Generated by the
 * `trajectory-reviewer` server module.
 */
export interface ClaudeHint {
  rubricId: string
  value: number | string | boolean
  reason: string
}

/** Map: stepId → hints (a step may have hints on multiple rubrics). */
export type ClaudeHintsByStep = Readonly<
  Record<string, readonly ClaudeHint[]>
>

/** Map: stepId → rubricId → my mark. The annotator's own current state. */
export type StepMarksByStep = Readonly<
  Record<string, Readonly<Record<string, Mark>>>
>

/** Map: rubricId → my mark (trajectory-level). */
export type TrajectoryMarks = Readonly<Record<string, Mark>>

/**
 * Three annotation modes. Mirror the design's segmented control:
 *
 *   standard — 3-pane (list / detail / rubrics) — default
 *   focus    — one step at a time + big Likert + auto-advance (long traces)
 *   compare  — two trajectories side-by-side with synced scroll (Arena mode)
 */
export type AnnotateMode = 'standard' | 'focus' | 'compare'
