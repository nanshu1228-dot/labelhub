import { z } from 'zod'

/**
 * Canonical trajectory schema — the internal truth.
 *
 * All ingest channels (Eval-Run / SDK / Upload) normalize to this shape via
 * adapters before persisting. Keep this stable; bump `schemaVersion` for
 * breaking changes.
 *
 * Design notes:
 *   - `parentStepId` exists from day 1 even though MVP renders linearly.
 *     Zero cost to keep, supports branching trees later (counterfactuals).
 *   - `content` is kind-discriminated; use `validateStepContent()` after parsing
 *     the outer shape to enforce kind-specific structure.
 *   - `tool_call` and `tool_result` share a `toolCallId` for pairing.
 *   - `providerKind` on tool calls is a HINT — the ingest layer is the
 *     authoritative resolver via the workspace's tool_providers catalog.
 */

// ───────────────────────────────────────────────────────────────────────
// Tool provider kinds (single source of truth — schema + Zod + types)
// ───────────────────────────────────────────────────────────────────────
export const TOOL_PROVIDER_KINDS = [
  'function',
  'mcp',
  'skill',
  'cli',
  'api',
] as const
export type ToolProviderKind = (typeof TOOL_PROVIDER_KINDS)[number]

// ───────────────────────────────────────────────────────────────────────
// Step kinds
// ───────────────────────────────────────────────────────────────────────
export const STEP_KINDS = [
  'thinking', // model reasoning / planning text
  'tool_call', // model invoking a tool
  'tool_result', // tool returning a result (paired by toolCallId)
  'sub_agent_call', // delegating to sub-agent
  'sub_agent_response',
  'final_response', // model's final output to user
  'error', // failure
] as const
export type StepKind = (typeof STEP_KINDS)[number]

// ───────────────────────────────────────────────────────────────────────
// Trajectory source
// ───────────────────────────────────────────────────────────────────────
export const TRAJECTORY_SOURCES = [
  'production', // SDK ingest from publisher's production
  'eval-run', // our managed Eval-Run endpoint
  'synthetic', // generated by us for benchmarks
  'upload', // manual JSON upload
] as const
export type TrajectorySource = (typeof TRAJECTORY_SOURCES)[number]

// ───────────────────────────────────────────────────────────────────────
// Per-kind content schemas
// ───────────────────────────────────────────────────────────────────────
export const thinkingContentSchema = z.object({
  text: z.string(),
})
export type ThinkingContent = z.infer<typeof thinkingContentSchema>

export const toolCallContentSchema = z.object({
  toolCallId: z.string().min(1),
  toolName: z.string().min(1),
  args: z.unknown(),
  /** Hint to the ingest layer about which tool_provider kind to resolve against. */
  providerKind: z.enum(TOOL_PROVIDER_KINDS).optional(),
  /** Kind-specific provider hints (resolver uses these for identifier construction). */
  providerHint: z
    .object({
      mcpServer: z.string().optional(),
      mcpCapability: z.string().optional(),
      cliCommand: z.string().optional(),
      skillName: z.string().optional(),
      apiOperationId: z.string().optional(),
    })
    .optional(),
})
export type ToolCallContent = z.infer<typeof toolCallContentSchema>

export const toolResultContentSchema = z.object({
  toolCallId: z.string().min(1),
  output: z.unknown(),
  isError: z.boolean().optional(),
  // CLI-specific shape (optional — only when kind=cli)
  stdout: z.string().optional(),
  stderr: z.string().optional(),
  exitCode: z.number().int().optional(),
})
export type ToolResultContent = z.infer<typeof toolResultContentSchema>

export const subAgentCallContentSchema = z.object({
  subAgentCallId: z.string().min(1),
  subAgentName: z.string().min(1),
  input: z.unknown(),
})
export type SubAgentCallContent = z.infer<typeof subAgentCallContentSchema>

export const subAgentResponseContentSchema = z.object({
  subAgentCallId: z.string().min(1),
  output: z.unknown(),
  isError: z.boolean().optional(),
})
export type SubAgentResponseContent = z.infer<
  typeof subAgentResponseContentSchema
>

export const finalResponseContentSchema = z.object({
  text: z.string(),
})
export type FinalResponseContent = z.infer<typeof finalResponseContentSchema>

export const errorContentSchema = z.object({
  message: z.string(),
  code: z.string().optional(),
  stack: z.string().optional(),
})
export type ErrorContent = z.infer<typeof errorContentSchema>

const CONTENT_SCHEMAS: Record<StepKind, z.ZodTypeAny> = {
  thinking: thinkingContentSchema,
  tool_call: toolCallContentSchema,
  tool_result: toolResultContentSchema,
  sub_agent_call: subAgentCallContentSchema,
  sub_agent_response: subAgentResponseContentSchema,
  final_response: finalResponseContentSchema,
  error: errorContentSchema,
}

// ───────────────────────────────────────────────────────────────────────
// Canonical step + trajectory
// ───────────────────────────────────────────────────────────────────────
export const canonicalStepSchema = z.object({
  sequence: z.number().int().nonnegative(),
  /** Null for root linear steps; set for branched / counterfactual sub-steps. */
  parentStepId: z.string().nullable().optional(),
  kind: z.enum(STEP_KINDS),
  /** Validated by kind via `validateStepContent` below. */
  content: z.unknown(),
  ts: z.string().datetime().optional(),
  latencyMs: z.number().nonnegative().optional(),
  tokensIn: z.number().int().nonnegative().optional(),
  tokensOut: z.number().int().nonnegative().optional(),
  modelName: z.string().max(120).optional(),
})
export type CanonicalStep = z.infer<typeof canonicalStepSchema>

export const canonicalTrajectorySchema = z.object({
  agentName: z.string().min(1).max(120),
  rootPrompt: z.string().min(1).max(50_000),
  finalResponse: z.string().max(200_000).optional(),
  source: z.enum(TRAJECTORY_SOURCES),
  meta: z.record(z.string(), z.unknown()).optional(),
  steps: z.array(canonicalStepSchema).min(1).max(2000),
  schemaVersion: z.literal('1.0').default('1.0'),
})
export type CanonicalTrajectory = z.infer<typeof canonicalTrajectorySchema>

/**
 * Two-stage validation: first the outer step shape (sequence, kind, metadata),
 * then the kind-specific content schema. Returns the parsed step or throws ZodError.
 */
export function validateStepContent(step: CanonicalStep): CanonicalStep {
  const contentSchema = CONTENT_SCHEMAS[step.kind]
  contentSchema.parse(step.content)
  return step
}

/**
 * Validate a whole trajectory including per-step content. Cheaper to call once
 * at the ingest boundary than to re-validate per write.
 */
export function validateTrajectory(input: unknown): CanonicalTrajectory {
  const trajectory = canonicalTrajectorySchema.parse(input)
  for (const step of trajectory.steps) {
    validateStepContent(step)
  }
  return trajectory
}
