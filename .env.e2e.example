# .env.e2e.example — doctor 深度层(`npm run doctor:deep`)所需环境
# ---------------------------------------------------------------------------
# 复制为 .env.e2e(已被 .gitignore,绝不入库),填入真实值。仅深度全链路
# (Tier 2)需要;Tier 0/1(`npm run doctor`)不需要任何凭据。
#
#   cp .env.e2e.example .env.e2e   # 然后填下面三项
#   npm run doctor:deep
#
# 工作原理:整站(应用 + Postgres + 鉴权)都在服务器上。深度层用 Playwright
# 【远程模式】直打线上(默认 aipert.top),用一个【专门的 smoke admin】登录,
# 只在一个【隔离的 smoke 工作区】里跑 领取→作答→提交→审批→payout→导出 全链路,
# payout 在该 admin 自己的 /my/earnings 页面(UI)断言。数据只落 smoke 工作区,
# 绝不碰 demo 数据;不连服务器 DB、不需要 Docker、不需要 service-role。

# 专门的 smoke 管理员(在线上 /signup 注册一次即可;务必只让它属于 smoke 工作区)
E2E_ADMIN_EMAIL=smoke-admin@example.com
E2E_ADMIN_PASSWORD=<that-account-password>

# 隔离 smoke 工作区的 uuid(登录后建一个新工作区 → 在里面建一个任务 → 导入几条
# 题目 → 发布;然后把该工作区 URL /workspaces/<这串> 里的 uuid 填到这里)
E2E_SMOKE_WORKSPACE_ID=00000000-0000-0000-0000-000000000000

# 可选:打非默认目标(默认跟随 `--target`,即 prod=https://aipert.top)
# E2E_BASE_URL=https://aipert.top

# ───────────────────────────────────────────────────────────────────────────
# 一次性前置(把 smoke 工作区备好,之后每次 `npm run doctor:deep` 直接跑):
#   1. 线上 /signup 注册 smoke admin(上面的邮箱/密码)。
#   2. 登录后新建一个工作区(名字如 “SMOKE · doctor”),记下它的 uuid。
#   3. 在该工作区建一个任务,导入几条题目并发布(留出可领取的题目)。
#   4. 把 uuid 填进 E2E_SMOKE_WORKSPACE_ID。
# 题目被领取/审批后会消耗,题少时再导入几条即可。
